# Frontend data source mode:
# - mock: static frontend data + localStorage overlay
# - api: FastAPI backend + PostgreSQL
VITE_DATA_SOURCE=mock

# Used only by api mode.
VITE_API_BASE_URL=http://127.0.0.1:8000
