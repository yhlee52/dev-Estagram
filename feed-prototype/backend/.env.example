APP_NAME=feed-prototype-backend
APP_ENV=local

# Local PostgreSQL database used by FastAPI, Alembic, seed, and import scripts.
# Use a different database name or --database-url override when separating test
# imports from an operational/update database.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/feed_prototype
